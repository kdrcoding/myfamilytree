name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run typecheck

      - name: Build
        run: npm run build
        env:
          # Vite base path must match the repository name for GitHub Pages.
          # If you rename the repository, nothing to change here - it is derived
          # automatically. For a user/organization page (username.github.io),
          # change this to "/".
          VITE_BASE_PATH: '/${{ github.event.repository.name }}/'
          # Supabase connection. These are PUBLIC values by design (they ship
          # in the browser bundle; access is controlled by RLS policies), so
          # they are safe to keep in this file. The service_role key must
          # never appear here.
          VITE_SUPABASE_URL: 'https://kasvrgqbmydypwvkqzju.supabase.co'
          VITE_SUPABASE_ANON_KEY: 'sb_publishable_RBsUVm4RisIKnH8fEXA7Gw_wZeHiGja'

      - name: Add SPA fallback for client-side routing
        run: cp dist/index.html dist/404.html

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
